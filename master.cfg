import slaves
reload(slaves)

import changesources
reload(changesources)

import schedulers
reload(schedulers)

import builders
reload(builders)

import statustargets
reload(statustargets)

import config
reload(config)

c = BuildmasterConfig = {}

c["title"] = "Sugar"
c["titleURL"] = "http://sugarlabs.org"
c["buildbotURL"] = config.config["url"]
c["db"] = {"db_url" : "sqlite:///state.sqlite"}

slaves.setup(c, config.config)
changesources.setup(c, config.config)
schedulers.setup(c, config.config)
builders.setup(c, config.config)
statustargets.setup(c, config.config)
